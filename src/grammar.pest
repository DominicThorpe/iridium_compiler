WHITESPACE = _{" " | "\n"}
number = {"0b" ~ '0'..'1'+ | "0x" ~ ASCII_HEX_DIGIT+ | ASCII_DIGIT+}
return_stmt = {"return" ~ number ~ ";"}
program = {SOI ~ "int" ~ "main" ~ "(" ~ ")" ~ "{" ~ return_stmt{0,1} ~ "}" ~ EOI}